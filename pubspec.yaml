name: super_market
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.7.3
  csv: ^6.0.0

  # ✅ ObjectBox (STABLE VERSION)
  objectbox: ^2.5.1
  objectbox_flutter_libs: ^2.5.1

  intl: ^0.20.2
  path: ^1.9.1
  path_provider: ^2.1.5

  mongo_dart: ^0.10.5

  device_info_plus: ^12.3.0
  barcode_widget: ^2.0.4
  pdf: ^3.11.1
  printing: ^5.13.4
  url_launcher: ^6.3.1
  fl_chart: ^0.70.2

  # ✅ IMPORTANT (compatible with mongo_dart)
  http: ^0.13.6

  share_plus: ^12.0.1

  # ✅ Excel (compatible version)
  excel: ^3.0.0

  file_picker: ^8.0.0+1
  analyzer: ^6.4.1
  window_manager: ^0.5.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # ✅ ObjectBox generator (MATCH VERSION)
  build_runner: ^2.3.3
  objectbox_generator: ^2.5.1
flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
  #   - assets/fonts/

